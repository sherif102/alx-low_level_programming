#include <stdio.h>

/**
 * main - main function used
 * @argc: number of arguments
 * @argv: arguments array list
 * Return: success
 */

int main(int argc, char **argv)
{
	printf("%d\n", --argc);

	return (0);
}
